Put call sound files here.

Required file names:
- incoming_call.mp3
- outgoing_ring.mp3
- call_ended.mp3

Existing files:
- message.mp3 is used for message notifications.
- call.mp3 is legacy and is no longer used by the call flow.
